interface:
  display_name: "One Step"
  short_description: "Repo-specific patterns and workflows for one-step"
  default_prompt: "Use the one-step repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true